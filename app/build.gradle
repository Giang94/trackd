plugins {
    id 'com.android.application'
    id 'org.jetbrains.kotlin.android' // needed for Room runtime
}

android {
    namespace 'com.app.trackd'
    compileSdk 36

    defaultConfig {
        applicationId "com.app.trackd"
        minSdk 34
        targetSdk 36
        versionCode 1
        versionName "1.0"

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_11
        targetCompatibility JavaVersion.VERSION_11
    }

    buildFeatures {
        compose false
    }
}

dependencies {
    // Material Components
    implementation "com.google.android.material:material:1.9.0"

    // RecyclerView & CardView
    implementation "androidx.recyclerview:recyclerview:1.4.0"
    implementation "androidx.cardview:cardview:1.0.0"

    // Glide for image loading
    implementation "com.github.bumptech.glide:glide:5.0.5"
    implementation 'com.google.android.flexbox:flexbox:3.0.0'

    // Room (Java only)
    implementation "androidx.room:room-runtime:2.6.1"
    implementation libs.material
    annotationProcessor "androidx.room:room-compiler:2.6.1"

    // CameraX
    implementation "androidx.camera:camera-core:1.3.0"
    implementation "androidx.camera:camera-camera2:1.3.0"
    implementation "androidx.camera:camera-lifecycle:1.3.0"
    implementation "androidx.camera:camera-view:1.3.0"

    // ZXing for barcode scanning (optional)
    implementation 'com.journeyapps:zxing-android-embedded:4.3.0'
    implementation("com.google.code.gson:gson:2.13.2")

    implementation project(':openCVLib')

    // Core TFLite runtime
    implementation 'org.tensorflow:tensorflow-lite:2.13.0'
    // TensorFlow Lite Task Library (vision / embedding)
    implementation 'org.tensorflow:tensorflow-lite-task-vision:0.4.4'
    // TensorFlow Lite Support Library (Makes handling Bitmaps easy)
    implementation 'org.tensorflow:tensorflow-lite-support:0.4.4'
}

// Force single annotations version to avoid duplicates
configurations.all {
    resolutionStrategy.eachDependency { details ->
        if (details.requested.group == 'org.jetbrains') {
            if (details.requested.name == 'annotations') {
                details.useVersion "23.0.0"
            }
        }
    }
}
